# 限制

脚本使用 `openpyxl`，适用于常规 XLSX 数据、公式文本和基础样式。它不会计算 Excel 公式，也不能承诺无损保留 VBA、透视表缓存、Power Query、外部连接、复杂图表、切片器或所有条件格式。

处理宏启用文件时必须使用 `keep_vba=True` 并在副本上验证。涉及上述原生功能时，只做最小变更，保留原始包内容，并在目标 Excel 引擎中打开、复算和检查后再交付。
