# 限制说明

现有脚本支持常规 DOCX 文本、标题、表格和标量占位符，不保证无损保留修订、批注、内容控件、嵌入对象、复杂域、图表、SmartArt 或宏。

遇到上述情形时，先在隔离运行目录中保护源文件，再做最小范围的 OOXML 修改，并在交付前渲染和比较结果。不得声称重新生成的文档保留了未支持的功能。

脚本依赖 `python-docx`。LibreOffice 是可选依赖，仅用于 PDF 渲染检查。
