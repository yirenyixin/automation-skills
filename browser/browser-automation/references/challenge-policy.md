# 验证挑战规则

当挑战检测发现 CAPTCHA、滑块验证、动态 iframe 挑战、reCAPTCHA、hCaptcha、Turnstile 或 OTP 验证时，在运行目录的 `evidence/` 下保留当前 URL、标签页标识、DOM 快照和截图。暂停工作流，并请用户在打开的浏览器中完成验证。统一适配器会拒绝挑战状态未明确为 `clear` 的交互请求。

用户报告完成后，重新检查挑战组件是否消失，以及预期会话或页面状态是否存在；确认后才从保存的检查点恢复。绝不尝试破解、绕过或规避 CAPTCHA。用户可以提供 OTP，工作流随后可填写它并验证结果状态。

